import { useEffect, useRef, useState } from "react";
import { Button } from "@/components/ui/button";
import { PageHeader } from "@/components/PageHeader";
import { Clock3, ExternalLink, Gamepad2, Instagram, MonitorPlay, ShieldAlert, Sparkles } from "lucide-react";
import { toast } from "sonner";

const resources = [
  { title: "DW عربية", detail: "محتوى تعليمي وفيديوهات مفيدة", url: "https://www.dw.com/ar/", kind: "محتوى" },
  { title: "Chess.com", detail: "استراحة ذكية وتحسين التفكير", url: "https://www.chess.com/", kind: "استراحة" },
  { title: "YouTube", detail: "للمحاضرات أو فيديو قصير", url: "https://www.youtube.com/", kind: "سوشيال" },
  { title: "Instagram", detail: "استراحة خفيفة بوقت محدد", url: "https://www.instagram.com/", kind: "سوشيال" },
  { title: "TikTok", detail: "استخدمه بحدود، مش هو اللي يستخدمك", url: "https://www.tiktok.com/", kind: "سوشيال" },
];
function clock(value: number) { return `${String(Math.floor(value / 60)).padStart(2, "0")}:${String(value % 60).padStart(2, "0")}`; }
export default function FocusLounge() { const [minutes, setMinutes] = useState(10); const [left, setLeft] = useState(0); const windowRef = useRef<Window | null>(null); const resourceRef = useRef<string | null>(null); useEffect(() => { if (!left) return; const id = window.setInterval(() => setLeft(value => Math.max(0, value - 1)), 1000); return () => clearInterval(id); }, [left]); useEffect(() => { if (left !== 0 || !resourceRef.current) return; try { windowRef.current?.close(); } catch { /* external windows may not be closeable */ } toast("خلص وقت الاستراحة — نرجع نكمّل يا بطل.", { description: "لو الموقع الخارجي لسه مفتوح، اقفله يدويًا وارجع للمذاكرة." }); resourceRef.current = null; windowRef.current = null; }, [left]); const launch = (resource: typeof resources[number]) => { if (left) { toast.error("خلص الجلسة الحالية الأول أو استنى المؤقت يخلص."); return; } const opened = window.open(resource.url, "_blank", "popup,width=1180,height=800"); if (opened) opened.opener = null; windowRef.current = opened; resourceRef.current = resource.title; setLeft(minutes * 60); toast.success(`اتفتحت ${resource.title} لمدة ${minutes} دقيقة.`); }; return <div><PageHeader title="استراحة محسوبة" description="افتح اللي تحتاجه برا الموقع، بس خلي وقتك في إيدك. لما المؤقت يخلص هنفكّرك ترجع." /><section className="mb-6 overflow-hidden rounded-3xl bg-gradient-to-br from-slate-950 via-violet-950 to-fuchsia-950 p-7 text-white"><div className="grid items-center gap-6 md:grid-cols-[1fr_.55fr]"><div><p className="flex items-center gap-2 text-sm font-bold text-fuchsia-200"><Sparkles className="size-4" />استراحة من غير جلد ذات</p><h2 className="mt-3 text-3xl font-black">وقت الراحة مش عدوك،<br />الضياع هو العدو.</h2><p className="mt-4 max-w-xl leading-7 text-slate-300">اختار وقتك، افتح الحاجة اللي عايزها، ولما المؤقت يخلص ارجع للمهمة اللي هتفرق معاك.</p></div><div className="rounded-3xl border border-white/10 bg-white/10 p-6 text-center backdrop-blur"><Clock3 className="mx-auto size-8 text-cyan-200" /><p className="mt-3 text-sm text-slate-300">الجلسة الحالية</p><p className="font-[Manrope] text-6xl font-black">{left ? clock(left) : "جاهز"}</p>{!left && <div className="mt-4 flex justify-center gap-2">{[5, 10, 15, 25].map(value => <Button key={value} variant={minutes === value ? "default" : "secondary"} size="sm" onClick={() => setMinutes(value)}>{value} د</Button>)}</div>}</div></div></section><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{resources.map(resource => <article key={resource.title} className="surface p-5"><div className="flex items-center justify-between"><span className="flex size-11 items-center justify-center rounded-2xl bg-primary/10 text-primary">{resource.title === "Chess.com" ? <Gamepad2 /> : resource.kind === "سوشيال" ? <Instagram /> : <MonitorPlay />}</span><span className="rounded-full bg-muted px-3 py-1 text-xs font-bold text-muted-foreground">{resource.kind}</span></div><h2 className="mt-5 text-lg font-extrabold">{resource.title}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-muted-foreground">{resource.detail}</p><Button className="mt-5 w-full" variant="outline" disabled={Boolean(left)} onClick={() => launch(resource)}><ExternalLink />افتح لمدة {minutes} د</Button></article>)}</div><p className="mt-5 flex items-start gap-2 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900"><ShieldAlert className="mt-0.5 size-5 shrink-0" />المؤقت يرسل تنبيهًا ويحاول إغلاق النافذة التي فتحها من هنا. بعض المتصفحات تمنع إغلاق المواقع الخارجية تلقائيًا، وقتها اقفلها بنفسك عند ظهور التنبيه.</p></div>; }
