import { Button } from "@/components/ui/button";
import { PageHeader } from "@/components/PageHeader";
import { useTheme } from "@/contexts/ThemeContext";
import { trpc } from "@/lib/trpc";
import { CalendarRange, Moon, Palette, ShieldCheck, Sun } from "lucide-react";
export default function Settings() { const { theme, toggleTheme } = useTheme(); const { data } = trpc.dashboard.summary.useQuery(); return <div><PageHeader title="الإعدادات" description="تحكم في تجربة العرض. تبقى بيانات الدراسة والمكافآت محمية على الخادم." /><div className="grid gap-4 lg:grid-cols-2"><section className="surface p-5"><div className="flex items-center gap-3"><span className="metric-icon bg-primary/10 text-primary"><Palette /></span><div><h2 className="font-bold">المظهر</h2><p className="text-sm text-muted-foreground">انتقل بين المظهر الفاتح والداكن.</p></div></div><Button className="mt-5" variant="outline" onClick={toggleTheme}>{theme === "dark" ? <Sun /> : <Moon />}{theme === "dark" ? "تفعيل المظهر الفاتح" : "تفعيل المظهر الداكن"}</Button></section><section className="surface p-5"><div className="flex items-center gap-3"><span className="metric-icon bg-violet-100 text-violet-700"><CalendarRange /></span><div><h2 className="font-bold">دورة الـ90 يوم</h2><p className="text-sm text-muted-foreground">{data?.cycle ? `تنتهي في ${new Date(data.cycle.endAt).toLocaleDateString("ar-EG")}` : "يتم إعدادها عند بدء النشاط."}</p></div></div><p className="mt-5 rounded-xl bg-muted/70 p-3 text-sm text-muted-foreground">تبقى الدورات السابقة محفوظة كسجل تاريخي؛ لا يتم حذف تقدمك عند بدء دورة جديدة.</p></section><section className="surface p-5 lg:col-span-2"><div className="flex items-center gap-3"><span className="metric-icon bg-emerald-100 text-emerald-700"><ShieldCheck /></span><div><h2 className="font-bold">سلامة البيانات</h2><p className="text-sm text-muted-foreground">تتحقق عمليات المكافآت والشراء والإكمال في الخادم مع مفاتيح مرجعية تمنع التكرار.</p></div></div></section></div></div>; }
