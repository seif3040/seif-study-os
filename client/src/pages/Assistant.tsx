import { useState } from "react";
import { AIChatBox, type Message } from "@/components/AIChatBox";
import { PageHeader } from "@/components/PageHeader";
import { errorText } from "@/lib/study";
import { trpc } from "@/lib/trpc";
import { BrainCircuit, Sparkles } from "lucide-react";
import { toast } from "sonner";
export default function Assistant() { const [messages, setMessages] = useState<Message[]>([]); const mutation = trpc.ai.chat.useMutation({ onSuccess: answer => setMessages(prev => [...prev, { role: "assistant", content: String(answer) }]), onError: e => toast.error(errorText(e)) }); const send = (content: string) => { const next = [...messages, { role: "user" as const, content }]; setMessages(next); mutation.mutate({ messages: next.map(({ role, content }) => ({ role: role as "user" | "assistant", content })) }); }; return <div><PageHeader title="المساعد الذكي" description="اسأل، راجع، أنشئ أسئلة، أو اطلب خطة مراجعة. المحادثة أداة دراسية ولا تمنح Coins." /><div className="grid gap-4 xl:grid-cols-[.34fr_1fr]"><aside className="surface p-5"><div className="metric-icon mb-4 bg-violet-100 text-violet-700"><BrainCircuit /></div><h2 className="font-bold">رفيق دراسة مركّز</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">سيجيب المساعد بالعربية الواضحة، مع الحفاظ على المصطلحات التقنية عند الحاجة.</p><div className="mt-5 space-y-2 text-sm"><p className="font-semibold">يمكنه مساعدتك في:</p><p className="text-muted-foreground">شرح المفاهيم، توليد أسئلة، تلخيص مادة، فهم الأخطاء، وخطط المراجعة.</p></div></aside><AIChatBox className="overflow-hidden" height="min(66vh, 680px)" messages={messages} onSendMessage={send} isLoading={mutation.isPending} placeholder="اكتب سؤالك الدراسي هنا..." emptyStateMessage="كيف يمكنني مساعدتك في مذاكرتك اليوم؟" suggestedPrompts={["اشرح لي مفهومًا صعبًا بأسلوب مبسط", "أنشئ 5 أسئلة للمراجعة", "ضع لي خطة مراجعة لمدة 3 أيام", "كيف أحلل أخطائي في امتحان؟"]} /></div></div>; }
